\tikz[vcenter, scale=0.4]{
    \tikzfixsize{(0,0)}{(4,4)}
    \squarecoord
    %
    \path
    (top) edge[string=dRed] (bot)
    (mid) edge[string=dRed] ($(mid) + (1,0)$);
    \node [enddot=dRed] at ($(mid) + (1,0)$) {};
}