\tikz[vcenter, scale=0.4]{
    \tikzfixsize{(0,0)}{(4,4)}
    \squarecoord
    %
    \path
    (mid) edge[string=dBlue] (top)
    (mid) edge[string=dBlue] (botl)
    (mid) edge[string=dBlue] (botr);
}