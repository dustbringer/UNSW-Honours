\tikz[vcenter, scale=0.25]{
    \tikzfixsize{(0,0)}{(4,4)}
    \squarecoord
    %
    \path
    (topl) edge[string=dRed] (mid)
    (topr) edge[string=dRed] (mid)
    (bot) edge[string=dRed] (mid);
    \path
    ($(mid) + (.5,1.5)$) edge[string=dRed] ($(mid) + (-.5,1.5)$);
    \node[enddot/scaled={dRed}{0.25}] at ($(mid) + (.5,1.5)$) {};
    \node[enddot/scaled={dRed}{0.25}] at ($(mid) + (-.5,1.5)$) {};
}