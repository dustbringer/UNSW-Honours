\tikz[vcenter, scale=0.4]{
    \squarecoord
    \tikzfixsize{(2,0)}{(2,4)}
    %
    \path
    (bot) edge[string=black] (mid);
    \node[box] at (mid) {$f_1$};
    \node[below] at (bot) {$a$};
    \node[above] at (top) {\phantom{$a$}};
}