\tikz[vcenter, scale=0.4]{
    \squarecoord
    \tikzfixsize{(2,0)}{(2,5)}
    %
    \path
    (bot) edge[string=black] (mid);
    \node[box] at (mid) {$f_1$};
    \coordinate[label=below:$a$] () at (bot);
}